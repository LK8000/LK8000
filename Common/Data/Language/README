- language.json contains list of available language
- Translation sub directory only contains language json files

- never remove unsed token, mark them deprecated by changing there contents
- never reuse deprecated token to avoid translation mistake.
- if string is not translated, remove them from translation files.

